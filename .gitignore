# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env

# Data folder: ignore everything inside data/, but keep the folder itself
# (Git doesn't track empty folders, so .gitkeep is used to track the folder)
data/*
!data/.gitkeep
